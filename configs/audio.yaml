defaults:
  - default.yaml
  - model: audio_codec
  - _self_


# Add audio encoder lr
optimizer:
  lr_audio: 5e-4

data_split_folder: null
window_seconds: 30

is_discrete: true
augment: false

sample_rate: 24000
grid_ms: 20

seed: 42

tags: [audio]
